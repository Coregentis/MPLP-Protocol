# Schema Conventions

## Naming Convention

All module schemas use the format:

`mplp-<module>.schema.json`

This naming rule is mandatory, permanent, and applies to all current and future versions.

Common schemas are located in `common/` and do not use the `mplp-` prefix.

